# BasicES6DevKit
Basic ES6 Development Kit with Babel, Globify and Browser Sync

```
# install node_modules
npm install

# run watcher
npm run watch

# run build makes a small minified and uglified .min.js file
npm run build
```